services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - db_data:/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  db_data:
