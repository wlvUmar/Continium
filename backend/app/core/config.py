"""
App configuration.

TODO:
- Define Settings (pydantic-settings) for:
  - DATABASE_URL
  - JWT_SECRET, JWT_ALG, ACCESS_TOKEN_TTL, REFRESH_TOKEN_TTL
  - ENV (dev/prod), DEBUG
- Load from environment variables (.env)
"""
